package day26_arrays;

public class Notes {
	public static void main(String[] args) {
		
		//String method:
		//-comparison: equals, equalsIsIgnoreCase, compareTo
		//-case conversion: toUpperCase, toLowerCase
		//-length
		//-get char from index: charAt(index)
		//-find the index of some character: indexOf(char)
		//-take a part of the string : substring(begin, end)
		//-isEmpty to check if string is empty
		//-startsWith, endsWith
		//-contains
		//-replace
		//-trim
		//-concat
		//-----------------------------------------------------
		//-toCharArray()- takes a string and builds a char[] array consisting of each character
		//-split -it is used to manipulate arrays.
		
		//-to update values in the array use loop
		//-
		
		String str="java";
		char [] letters=str.toCharArray();
		// char[] letters= {'j','a','v','a'};
	}
}
