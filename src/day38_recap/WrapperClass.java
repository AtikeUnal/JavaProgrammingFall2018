package day38_recap;

public class WrapperClass {
/**
 * wrapper classes are existing to make an object out of primitives and manipulate them as an object.
 * -->unboxing: conversion from wrapper class to primitive type. It happens implicitly.
 * 
 * -->autoboxing: conversion from primitive type to wrapper class. Also happens implicitly.
 * 
 * ex int i=10;
 * Integer i2=i; --autoboxing
 * i.doSomething();
 * Integer ii=50;
 * 
 */
}
