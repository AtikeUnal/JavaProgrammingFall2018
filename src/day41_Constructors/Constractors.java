package day41_Constructors;

public class Constractors {
	
	/**
	 * Student s=new Student();--> Constructor which creates objects. It must have same name with class. 
	 * 
	 * Consturctor: A special kind of method!(acutally it is not a method) invoked every time an object is created.
	 *-It has same name as class
	 *-It doesnt have a return type 
	 * 
	 * Whetever accepts the word new in front is constructor ex: Scanner, Random, String
	 * 
	 * String b=new String();
	 * Scanner scan=new Scanner(System.in);
	 * 
	 */
}
